# Mac mini setup

Cleanup is verified. Only /Users/aman/Development/ente remains: clean main ef563ca9194a7c74336a331771cbb193ae52e466, one local branch, one worktree. Exact ente-2 is absent. Six secondary worktrees and eight old local task branches were removed across both passes. Product remote branches and PRs were preserved. Identified old implementation tasks were archived; Codex list_projects now returns only the retained ente project. Native Codex UI control is unavailable, so this is API evidence rather than a sidebar screenshot.

All 11 mini pilot queue entries are archived using the source hub's tested archived state, preserving IDs and history while preventing restart. The superseded cancellation helper on aman/mac-mini-consolidation is not installed. Current pilot dispatch has an empty task list and only retained ente routing; dispatch-20260922-cancelled.json keeps historical assignments. Latest mini PRD/BOARD changes were preserved during installation.

The live vault is now a Git repository on reconciled private main 6c00fcc322993eb131e9d6948c3221abd186b27b. Portable skills were installed while preserving original evidence, raw local review inputs, Obsidian settings and newer task notes. Source access is private AmanRajSinghMourya/ente-workflow, authenticated account aman-pilot with WRITE. Manual sync, service installation and Obsidian controls verification are in progress.

Recovery is retained at /Users/aman/Documents/Codex/ente-recovery-20260922T075859Z. Compact bundles and patches verify against retained ente. Original six bundles plus patches total 106,045 bytes; three further task-branch bundles preserve the remaining removed branch tips. Credentials/local config were preserved under /Users/aman/.local/share/ente-preserved-local-config-20260922 and were not uploaded. No large build or source backup was created.

The removed secondary worktrees occupied 18.06 GiB allocated, including 8.31 GiB Rust targets. Removed ente-2 occupied another 1.31 GiB. Observed filesystem free-space changes include concurrent activity and are not exact attribution. Toolchains and unrelated projects remain untouched.

Pickup is currently PAUSED until setup verification finishes, then only new explicit tasks may be picked up in retained ente under normal approval gates. PR-learning and cleanup jobs remain PAUSED; control.json still holds 2026-09-23T12:30:00Z with audit-only cleanup. Old pilot authorization cannot restart abandoned tasks.
