# Mac mini setup

Private workflow access works for aman-pilot (WRITE). The original import was pushed on aman/mac-mini-import at c018c0a, and this commit updates the final cleanup status and mini queue/task records. The live vault remains authoritative. Source hub must reconcile portable main with the raw mini skill differences before the live sync/controls installation; no LaunchAgent or Obsidian controls have been installed yet. Source-task messaging is unavailable cross-host, so this existing status record and task title are the handoff.

Aman's one-time local task cleanup is complete: six secondary worktrees and five associated local branches removed; eight relevant tasks archived, including the already archived slideshow task. Five SwiftPM queue rows are deferred/cancelled by user, never done. Pickup remains PAUSED; review-learning and cleanup remain PAUSED. control.json still holds 2026-09-23T12:30:00Z and audit-only cleanup. Do not recreate abandoned tasks from historical pilot authorization.

Both primary checkouts are preserved and clean at their original HEADs: ente main ef563ca9194a7c74336a331771cbb193ae52e466; ente-2 aman/locker-sync-fix 6393d8561a77e34654b16a8f9d0598cce6bb4557. Main/default branches, remote branches/PRs, credentials, notes/reviews/evidence and unrelated data are unchanged. Unmatched branches all and aman/server-drop-logging are deliberately retained. No product push or PR.

Recovery: /Users/aman/Documents/Codex/ente-local-task-recovery-20260922T075859Z. Six bundles plus binary patches total 106,045 bytes; bundle prerequisites are in the preserved primary repos. Every bundle passed verification and every patch reconstructed its exact head tree. RESTORE.md and recovery-manifest.json describe recovery. Archiving the two Codex-managed clean tasks automatically removed their directories; the commits remained in the primary repos and were successfully backed up. Four ordinary task worktrees were backed up before Git-aware removal.

Removed worktrees accounted for 18.06 GiB allocated, including 8.31 GiB Rust targets. Filesystem free space rose 19.57 GiB during the operation, including concurrent activity. Final primary sizes: ente 1.44 GiB; ente-2 1.31 GiB. Shared caches remain: Xcode DerivedData 14.22 GiB, Cargo registry/git 1.69 GiB, pub 1.67 GiB, Flutter SDK 1.64 GiB. Full read-only inventory is in the recovery folder; raw process logs are not imported.

Recommendation: one base repo plus isolated task worktrees; retire ente-2 only under separate approval. Cargokit passes --target-dir from Xcode/Gradle build directories, so CARGO_TARGET_DIR alone does not relocate all artifacts. Consider a separately reviewed shared compiler cache; keep generated FRB/native/app outputs local to each task. No Rust symlink/cache policy or recurring disk cleanup was implemented.

Next owned by source hub: reconcile this import with portable main, retaining mini task authority and the approved inclusion of personal skills. Notify existing setup task 01a0c6ea-8e55-7c00-8798-52d124669141 when main is ready. Mini can then safely install repository sync and task controls using machine=mac-mini and both checkouts. Vault opening remains pending if locked, without an unlock request. Do not directly deploy the historical mini queue.py/review helper copies from this import over newer portable helpers.
